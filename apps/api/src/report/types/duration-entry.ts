export interface DurationEntry {
  stage?: string;
  milestone?: string;
  field?: string;
  Mean?: number;
  Mode?: number;
  Median?: number;
  count?: number; // number of applicants counted for the stats
}
